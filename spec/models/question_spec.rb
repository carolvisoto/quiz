require 'rails_helper'

RSpec.describe Question, :type => :model do
  pending "add some examples to (or delete) #{__FILE__}"
  # before { @question = Question.new(question: "Do you love dogs?", animal: "dog") }

 
  # it "should respond to 'question'" do
  #     expect(@question).to respond_to(:question)
  # end
  
  # it "should respond to 'animal'" do
  #     expect(@question).to respond_to(:animal)
  # end
end
